#include <stdio.h>

int main() {
    int n,i, sum=0;
    printf("Enter the number of terms in the series: ");
    scanf("%d", &n);
    for (i = 2; i <= n; i += 2) {
        sum += i;
    }
    printf("Sum of even numbers in the series is: %d\n", sum);
    return 0;
}
