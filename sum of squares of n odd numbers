#include <stdio.h>

int main() {
    int n,i,odd, sum=0;
    printf("Enter the number of terms in the series: ");
    scanf("%d", &n);
    
    for (i=1; i<=n; i++) {
        odd = 2*i - 1;
        sum += odd;
    }
    
    printf("The sum of the odd number series is %d", sum);
    return 0;
}
